const { createTask, modifyTask, deleteTask, getTasks } = require("./task");

async function wrikeRouting(obj) {
  // put in here a way to see what's changing from the graph API to make the change in Wrike
}

module.exports = wrikeRouting;
